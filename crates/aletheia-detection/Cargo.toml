[package]
name = "aletheia-detection"
description = "Scripture detection contracts, confidence policy, and parser skeleton for Aletheia."
authors.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
aletheia-core = { path = "../aletheia-core" }

[lib]
path = "src/lib.rs"

[lints]
workspace = true

