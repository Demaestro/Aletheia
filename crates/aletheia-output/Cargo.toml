[package]
name = "aletheia-output"
description = "Presentation output contracts for HDMI, NDI, OBS, vMix, and automation adapters."
authors.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
aletheia-core = { path = "../aletheia-core" }

[lib]
path = "src/lib.rs"

[lints]
workspace = true

